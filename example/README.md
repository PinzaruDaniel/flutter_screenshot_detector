# flutter_screenshot_detector_example

Simple runnable app for testing `flutter_screenshot_detector`.

## Run

From this folder:

```sh
flutter run
```

Take a screenshot and the counter should increment.

On Android, grant Photos and videos access in system settings if the device
requires it for screenshot detection.
